---
name: user preferences
description: Roman's tooling preferences and working style observed in this project
type: user
---

- Uses **uv** for Python package management (not pip/venv directly)
- Prefers **Python** over Go/Rust for scripting tasks — practical, not performance-driven
- Wants human-readable output alongside raw data (asked for markdown export)
- Uses **bat** as a `cat` replacement (alias `cat="bat --paging=never"` in `~/.zshrc`)
- Prefers simple, minimal approaches — pushed back on Playwright and mitmproxy as too complex
- Asks clarifying "why" questions before accepting a tool or approach
